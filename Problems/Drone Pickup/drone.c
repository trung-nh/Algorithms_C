// https://codeforces.com/group/Ir5CI6f3FD/contest/276073/problem/L

#include <stdio.h>

int main()
{
    int i, n, K;
    FILE *fptr = fopen("input.txt", "r");
    scanf(fptr, "%d%d", &n, &K);
    int goods[n], energy[n];
    return 0;
}